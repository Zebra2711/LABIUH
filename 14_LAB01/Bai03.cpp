#include <stdio.h> 
int main() 
{
      int x, y, sum;
      printf("Input the first integer: ");
      scanf("%d", &x);
      printf("\nInput the second integer: ");
      scanf("%d", &y);
      sum = x + y;
      printf("\nSum of the above two integers = %d", sum);
      return 0;
}
